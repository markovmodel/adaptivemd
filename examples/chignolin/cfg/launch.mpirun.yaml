# Command wrapping mpirun, or mpirun
command: "mpirun"

# Options to control launcher, assign hardware
resource:
    #--map-by:     "ppr:1:{task.bind_element}:PE={task.cpu_per_task}"
    -n:           "{task.n_tasks}"

# Additional to accommadate rigid formats
arguments:
    - "-display-map"
    - "-display-allocation"
