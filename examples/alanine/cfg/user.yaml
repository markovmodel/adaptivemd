
# User-specific needed to submit jobs
allocation: "bif112"

# These are not yet used, but could help shape
# workflow and prevent undesired useage
limit:      "5000"
formula:    "{nnodes}*{minutes}"
